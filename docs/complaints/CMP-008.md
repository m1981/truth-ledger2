# CMP-008 — a retracted figure kept living in four places, one of them a test asserting it

Sentence:   "2329 whisper lines / 6.8 claims per edit" — withdrawn by
            v1's J-040 (correct: 1670 / 22.6; the error summed per
            file while whisper emits one line per claim).
Noticed:    2026-08-18 (correction issued 08-17), v1 journal J-044
            ZNALEZISKO 4: the correction never reached
            template/truthlib/registry.py:69, gates.py:140,
            test-truth-core.py:5373, or .truth/watch-policies:30.
            Detected only when a later session went looking.
Cost:       A retracted number survived in production code, a shipped
            policy file, and a test that PINNED it — the suite defended
            the falsehood. The understated ratio (3×+) sized the
            whisper fatigue budget wrong. [verified by harvest agent,
            2026-09-01; class F-5]
Disposition: open
