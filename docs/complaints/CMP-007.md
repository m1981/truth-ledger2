# CMP-007 — a shipped comment measured the wrong constant; two health gates dead at 1/16th of the stated margin

Sentence:   "JSON travels via env vars — fine at current ledger size;
            revisit before the ledger approaches ARG_MAX (~1MB on
            macOS)" (v1: template/scripts/spec-health.sh:37,
            scripts/fact-health.sh:76).
Noticed:    2026-08-16, v1 journal J-018
            (docs/refactor/01-JOURNAL.md:541). The binding limit is
            MAX_ARG_STRLEN = 128 KiB, sixteen times smaller; the gates
            died when the ledger crossed 128 KiB (at 142 KiB), not at
            ~1 MB — invisible until hooks were armed in J-015.
Cost:       Two health gates dead from the crossing onward — one of
            them Tier A, shipped to consumers; the safety margin
            overstated by more than an order of magnitude in the
            script's own self-description. [verified by harvest agent,
            2026-09-01; class F-4]
Disposition: bought scripts/tl2 verify — the first F-4 instance: brief
            generation (the duty to go beyond the recipe) and the
            report-intake gate (marker, independence-limit line, valid
            verdict, or refused — the only gating part per
            architecture §6); dispatch criteria and choreography in
            docs/runbooks/verifier.md (2026-09-01), countersigned by
            the operator
