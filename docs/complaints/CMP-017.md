# CMP-017 — a one-paragraph edit to a watched source reds `check` permanently

Sentence:   The implicit claim of the capsule guard set: that a guarded
            document can be maintained. Editing docs/candidates.md (a
            doctrine note inside F-5, count of rows unchanged) makes
            cap-ff44a0be SUSPECT — correctly — but the store is
            append-only and no retraction verb exists, so the old
            capsule can never be tombstoned: `tl2 check` stays exit 1
            forever, even after a successor capsule is filed at the
            new anchor.
Noticed:    2026-09-01, by the author, in the act: the edit was made
            knowingly to surface this gap, which had been an orphaned
            observation of the session (v1's retraction ceremony —
            tombstone with cause and successor, COPE-shaped — was
            never inherited by v2).
Cost:       A permanently red check trains every reader to scroll past
            red — the exact death v1 measured on its six permanent
            ADR-008 warnings (harvest, mechanical row 10). One
            unretirable SUSPECT row per legitimate edit of any watched
            document, growing without bound.
Disposition: open (candidate purchase: a `tl2 retract` verb —
            append-only tombstone naming cause and successor id, honored
            by `check` and `whisper`)
