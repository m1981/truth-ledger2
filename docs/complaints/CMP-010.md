# CMP-010 — the canary, run from a linked worktree, wrote into the real repository

Sentence:   The canary sandbox's implicit claim of isolation: mkrepo()
            in v1's template/scripts/truth-canary.sh had a bare
            `cd "$1"` with no `|| exit`, under set -u and no set -e,
            so `git init -b main .` ran wherever the shell stood —
            and linked worktrees share .git/config.
Noticed:    ~2026-08-20 (measured 21:48), v1 journal J-047 §B; fixed
            441de48 (2026-08-21).
Cost:       core.hooksPath deleted (ALL gates inert ~4 days),
            core.bare=true broke the main working tree, six fixture
            branches, a worktree ref repointed — and four commits by
            two sessions authored as `test-actor <test@example.com>`:
            config and refs are restorable, a wrong author in published
            history is not. [verified by harvest agent, 2026-09-01;
            class F-7 — dev-tier machinery escaping into the real
            surface]
Disposition: open
