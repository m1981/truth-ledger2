# CMP-014 — a register carried review dates that never had a reader

Sentence:   v1's docs/governance/gate-metrics.md "Next review" column —
            the implicit claim that somebody or something reads those
            dates.
Noticed:    Column present since 6f3e9b1 (2026-08-02); measured
            2026-09-01 by harvest: six rows already past due
            (`grep -n '2026-08-08'`), and `grep -rn 'Next review'
            scripts/ instruments/ template/ .githooks/` returns
            nothing — no instrument, hook, or script ever consumed
            the column in its entire life.
Cost:       Every "Next review" date was a promise with no promisee;
            the register that prices gates by their record was itself
            priced by nobody. This is the exact death truth-ledger2's
            own probation-sweep prose rule (gate-metrics.md, named
            tension) is one unattended date away from. [verified by
            harvest agent, 2026-09-01; class NONE — no candidate
            covers "a register without a reader"; catalog-falsifier
            evidence, pairs with CMP-013]
Disposition: bought docs/candidates.md row F-10 (guard liveness) —
            a catalog amendment via the falsifier clause, jointly with
            CMP-013 (2026-09-01), countersigned by the operator
