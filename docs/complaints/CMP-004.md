# CMP-004 — "extras may run in any order" was false; zero rows seeded, silently

Sentence:   "Seeding extras may run in any order" (consumer repo,
            catalog-service.md) — seed_curated_kitchens creates the
            style_tags that seed_decor_style_tags links to.
Noticed:    2026-07-27, consumer question bank QB-001
            (kuchnie: docs/question-bank.md:11-14; closed by
            tr-0dda200b; worked example in docs/incident-to-gap.md).
            Wrong order produced 0 associations with no error; caught
            only by a scratch rebuild diffed against production.
Cost:       Silent empty join table in production seeding; doc
            correction plus a full rebuild re-verification.
            [verified against primary records by harvest agent,
            2026-09-01; class F-1]
Disposition: bought scripts/tl2 lint — the first F-1 instance,
            PROPOSER ONLY per the measured recommendation (the ruled
            gating layer produced zero signal over 21 consumer specs
            and misses this complaint's own sentence, so it stays
            unbought): mechanical extraction suggests candidate lines,
            a fresh session judges and drafts SC- assertions, the
            intake gate checks only the report's shape, and a human
            accepts proposals into the spec or not (2026-09-01),
            countersigned by the operator
