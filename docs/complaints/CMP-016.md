# CMP-016 — a line removed in one change was silently depended on by a later change

Sentence:   The implicit claim of a revert: that removing lines of a
            dead design removes only the dead design. The `unset` that
            would have closed the 2026-08-28 push-boundary bypass had
            existed — and was deleted by the same hand while reverting
            an unrelated design.
Noticed:    2026-08-28, v1 catch-log MISS row and
            docs/reviews/battery-nested-stub-review-2026-08-28.md:
            the revert saw a line belonging to a dead design; the
            later stub saw an env var that already meant "nested";
            neither step saw the dependency between them.
Cost:       Re-opened a live bypass of the entire integration suite at
            the push boundary. [verified by harvest agent, 2026-09-01;
            class NONE — the missing channel is decision→dependent-work
            propagation, twin of the decision→dependent-claims gap the
            same repo measured (ADR-057's two-week false claims);
            catalog-falsifier evidence]
Disposition: open
