# CMP-005 — 36 seeded variants offered by one read surface, hidden by another

Sentence:   The U-U seeding spec's implicit claim that seeded variants
            are reachable through the catalog's read surfaces — shipped
            with no read-surface-parity assertion.
Noticed:    2026-07-27, still open: wk-bca0a74b, QB-004 (kuchnie:
            docs/question-bank.md:26-29, STATUS.md:120). The variants
            have no worktop_specs rows, so v_worktops_full and the
            worktops endpoint hide them while the configurator's role
            fallback offers them.
Cost:       A configurator can quote a worktop the worktops endpoint
            says does not exist; 36 variants unreachable through the
            intended surface. [verified by harvest agent, 2026-09-01;
            class F-2]
Disposition: bought scripts/tl2 mirror — the first F-2 instance:
            manifest ids against cited ids, both directions always
            (a GAP or an ORPHAN reds), because this harm was exactly
            a missing parity assertion between two surfaces
            (2026-09-01), countersigned by the operator
