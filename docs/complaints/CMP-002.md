# CMP-002 — a review document went stale within an hour, detected by accident

Sentence:   An external review's findings about this repository at
            commit e31cf54 (among them: "docs/features.md defines the
            lifecycle", "F-6 has status BUILT", "README says four
            refusals") — written as true statements about the
            repository they described.
Noticed:    2026-08-31, by the reviewer, and only by accident: a grep
            for a cited file (features.md) returned nothing, because
            three commits had landed while the review was being
            written. No mechanism detected the drift; a tool failure
            did. The reviewer named it correctly: this is the exact
            question the product exists to answer — "does a written
            sentence still correspond to the repository it describes?"
            — answered "no" within one hour, on a live document.
Cost:       Three of eight findings were re-litigated stale; reviewer
            and operator time spent separating live findings from dead
            ones. First measured instance of the product thesis inside
            this project's own life — until now every complaint and
            purchase concerned repository hygiene, none concerned
            correspondence drift.
Disposition: bought scripts/tl2 — the first F-5 instance: `capsule`
            files sentence + command + output sha256 + commit anchor +
            watched paths; `check` recomputes SUSPECT/STALE/OK at read
            time (2026-08-31), countersigned by the operator
