# Verification report — cap-45714053 (2026-09-01)

Dispatched per runbooks/verifier.md (criteria 1+2+3 held:
self-certified by the author of the gate, the corpus, and the capsule;
judgment-above-recipe required; load-bearing metric-register claim).
Verifier: fresh session, no author context (rule-9 floor). Report
verbatim below; intake stamp at the end.

---

proposed-by: llm
limit: independent in context, not in failure — the reviewer shares the author model's blind spots
verdict: agree
basis: Re-ran the recorded recipe from the repo root: stdout was `0` and its sha256 (9a271f2a916b0b6ee6cecb2426f0b3206ef074578be55d9bc94f6f3fe3ab86aa) matches the capsule's output_sha256 exactly. Checked the anchor e0c1490: it is the CMP-002 commit that created docs/governance/commit-msg-corpus.txt (10 lines, +10 in the diffstat) as a replayable artifact; the hook itself predates the commit and is untouched by it, which is consistent with the sentence ("the gate passes its corpus"), and both watched paths exist unchanged since. So the scoped sentence — zero false alarms on *its 10-message corpus* — is true and reproducible. However, on duty (3) the corpus is a weak instrument for the wider "zero false alarms" reading: every corpus line is pure ASCII, and the hook's sole detector is Polish diacritics, so the corpus is structurally incapable of triggering the detector at all — it exercises the retired word-list failure mode (three Jest messages) but is vacuous against the live one. I invented eight legitimate English messages and ran each through `sh scripts/githooks/commit-msg`; six were blocked: crediting contributors "Michał Nowak"/"Paweł", adding "Łódź and Kraków" to city-name fixtures, a commit message about the hook itself ("also reject the ż and ź diacritics"), "Normalize ó to o in slug generation", and "uppercase Polish diacritic Ś in transliteration table". These are false alarms by the hook's own definition (its stderr anticipates them and points at gate-metrics.md). So: agree with the sentence as literally scoped, but the corpus should not be cited as evidence of a zero-false-alarm property over the domain of plausible legitimate English commits — any commit naming a Polish person, place, or the diacritic characters themselves will be blocked, and the corpus contains no case that could reveal this.

---
intake: accepted 2026-09-01 (tl2 verify --intake); disposition: pending operator ruling on the corpus-vacuity finding.
Operator ruling (b), 2026-09-01: detector narrowed to >=2 diacritic words; 4 of the 6 measured false alarms eliminated, 2 residual accepted, new single-word miss class documented. Disposition closed.
