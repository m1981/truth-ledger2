# Bootstrap for AI agents — truth-ledger2

You are in a repository that is its own memory: everything a previous
session knew that matters is HERE, not in any conversation. Reconstruct
in this order (minutes, not hours):

1. **`AGENTS.md`** — 12 conduct rules; rules 8 (certainty labels
   [S]/[M]/[I] with sources), 10 (decisions batched and kind-labeled:
   RULING / JUDGEMENT / SPECIFICATION, one question pending at a time),
   and 11 (every actionable finding terminates: robię / Twoja decyzja /
   odpuszczam — z powodem) govern how you answer the operator.
2. **`README.md`** — the read order (law → reasoning → choreographies →
   direction → state). Law is `docs/scope.md` + the constitution;
   nothing enters without a complaint (C1), nothing gates without both
   witnesses (C2) and a metric row with probation (C3).
3. **`docs/runbooks/session.md`** — how to open: derive the queue with
   `./scripts/tl2 ready`, then `./scripts/tl2 whisper <paths you plan
   to touch>` before editing anything (capsule guards will name what
   your edit endangers, tagged by criticality). How to close: check
   green, work staged, decision package.
4. The other runbooks choreograph everything else: `purchase.md`,
   `maintenance-loop.md` (successor-first; retract ids from the
   CURRENT check, never from memory), `consumer.md` (the
   two-repository contract with `~/PycharmProjects/kuchnie`,
   installation #2), `release.md`, `verifier.md`.

Conversation with the operator is Polish; everything committed is
English (rule 7 — a machine checks your commit message). The operator
countersigns purchases and rules on law; you stage, batch decisions,
and never let prose outrun truth (rule 12).

Sister repositories: `~/PycharmProjects/truth-ledger` (v1 — a frozen
archive and the harvest's evidence mine) and `~/PycharmProjects/kuchnie`
(the consumer, installation #2 — its v1 ledger is frozen per its
`.truth/FROZEN.md`; push BOTH repos when a session touches both).
